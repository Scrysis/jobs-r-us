{{#each jobReviews as |reviews|}}
<h1>Reviews of {{job_title}}</h1>
<section>
    <h3>{{first_name}}</h3>
    <h3>{{last_name}}</h3>
</section>
<section>
    <p>{{review_rating}}</p>
</section>
<section>
    <p>{{review_text}}</p>
</section>
{{/each}}